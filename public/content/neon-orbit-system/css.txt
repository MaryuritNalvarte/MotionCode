*, *::before, *::after { box-sizing: border-box; margin: 0; }

body {
  min-height: 100vh;
  display: grid;
  place-items: center;
  background: #050816;
  overflow: hidden;
}

.scene {
  position: relative;
  width: 400px;
  height: 400px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.orbit-system {
  position: relative;
  transition: transform 0.1s ease;
}

.core {
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: radial-gradient(circle, #fff 0%, #8b5cf6 60%);
  box-shadow:
    0 0 10px #8b5cf6,
    0 0 30px #8b5cf6,
    0 0 60px rgba(139, 92, 246, 0.5);
  position: relative;
  z-index: 10;
}

.core-pulse {
  position: absolute;
  inset: -8px;
  border-radius: 50%;
  border: 1px solid rgba(139, 92, 246, 0.4);
  animation: corePulse 2s ease-in-out infinite;
}

.ring {
  position: absolute;
  border-radius: 50%;
  border: 1px solid;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  top: 50%; left: 50%;
  transform-origin: center;
}

.ring-1 { width: 100px; height: 100px; margin: -50px 0 0 -50px; border-color: rgba(139,92,246,0.4); animation: orbit 3s linear infinite; }
.ring-2 { width: 160px; height: 160px; margin: -80px 0 0 -80px; border-color: rgba(59,130,246,0.4); animation: orbit 6s linear infinite reverse; }
.ring-3 { width: 220px; height: 220px; margin: -110px 0 0 -110px; border-color: rgba(236,72,153,0.4); animation: orbit 10s linear infinite; }

.planet { width: 10px; height: 10px; border-radius: 50%; margin-top: -5px; }
.planet-1 { background: #8b5cf6; box-shadow: 0 0 6px #8b5cf6, 0 0 12px rgba(139,92,246,0.6); }
.planet-2 { background: #3b82f6; box-shadow: 0 0 6px #3b82f6, 0 0 12px rgba(59,130,246,0.6); }
.planet-3 { background: #ec4899; box-shadow: 0 0 6px #ec4899, 0 0 12px rgba(236,72,153,0.6); }

@keyframes orbit { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
@keyframes corePulse { 0%,100%{transform:scale(1);opacity:0.6} 50%{transform:scale(2);opacity:0} }
